'use client';
import PlaceholderPage from '@/components/PlaceholderPage';
import { BookOpen } from 'lucide-react';
export default function DocsPage() {
  return <PlaceholderPage title="Docs" description="Documentation hub for agent protocols, API references, and guides." icon={BookOpen} />;
}
