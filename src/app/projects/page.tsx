'use client';
import PlaceholderPage from '@/components/PlaceholderPage';
import { FolderKanban } from 'lucide-react';
export default function ProjectsPage() {
  return <PlaceholderPage title="Projects" description="Track active projects, milestones, and deliverables across all agents." icon={FolderKanban} />;
}
