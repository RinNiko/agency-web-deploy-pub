'use client';
import PlaceholderPage from '@/components/PlaceholderPage';
import { Brain } from 'lucide-react';
export default function MemoryPage() {
  return <PlaceholderPage title="Memory" description="Browse and search agent memory banks, context, and learned patterns." icon={Brain} />;
}
