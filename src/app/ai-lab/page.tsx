'use client';
import PlaceholderPage from '@/components/PlaceholderPage';
import { FlaskConical } from 'lucide-react';
export default function AILabPage() {
  return <PlaceholderPage title="AI Lab" description="Experimental playground for testing new agent capabilities, prompts, and models." icon={FlaskConical} />;
}
