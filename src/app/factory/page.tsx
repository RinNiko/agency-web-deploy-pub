'use client';
import PlaceholderPage from '@/components/PlaceholderPage';
import { Factory } from 'lucide-react';
export default function FactoryPage() {
  return <PlaceholderPage title="Factory" description="Task factory for creating, templating, and automating agent workflows." icon={Factory} />;
}
